// Debug config — used for simulator and local device builds.
// Change RELAY_HOST to your Mac's LAN IP when running on a physical device.
// Simulator: 127.0.0.1
// Physical device: run `ipconfig getifaddr en0` on your Mac
RELAY_HOST = 127.0.0.1
RELAY_PORT = 8080
