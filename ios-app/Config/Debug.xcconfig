// Debug config — used for simulator and local device builds.
// Points at the prod relay so physical device builds work without a local server.
// To run against a local relay: set RELAY_HOST = <your Mac LAN IP>, RELAY_PORT = 8080
RELAY_HOST = versa.cslade.space
RELAY_PORT = 443
